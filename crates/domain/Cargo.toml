[package]
name = "ora-domain"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
pretty_assertions = "1"
